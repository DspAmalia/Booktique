@page "/antique/create"
@rendermode InteractiveServer
@using Microsoft.EntityFrameworkCore
@using Booktique.Models.MainModels
@using System.Security.Claims
@inject IDbContextFactory<BooktiqueContext> DbFactory
@inject NavigationManager NavigationManager
@inject AuthenticationStateProvider AuthStateProvider

<PageTitle>Register New Artifact | Antique Booktique </PageTitle>

<div class="antique-create-bg py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-9">

                <div class="mb-4">
                    <a href="/antique" class="btn-quill text-decoration-none">
                        <i class="bi bi-arrow-left me-2"></i>Return to Archives
                    </a>
                </div>

                <div class="inventory-scroll p-4 p-md-5">
                    <div class="text-center mb-5 border-bottom border-dark pb-4">
                        <h5 class="archive-label text-uppercase">Official Registry Entry</h5>
                        <h1 class="archive-title">Book Acquisition Record</h1>
                        <p class="archive-author fs-6">Documenting a new addition to our collective heritage</p>
                    </div>

                    <EditForm Model="NewBook" OnValidSubmit="HandleSubmit" FormName="create-heritage">
                        <DataAnnotationsValidator />
                        <ValidationSummary class="text-danger small mb-4" />

                        <div class="row g-4">
                            @* I. Identificare *@
                            <div class="col-12">
                                <h6 class="registry-section-title"><i class="bi bi-book me-2"></i>I. Tome Identification</h6>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="font-serif fw-bold text-brown-dark">Title</label>
                                        <InputText @bind-Value="NewBook.BookTitle" class="form-control ink-input" />
                                        <ValidationMessage For="() => NewBook.BookTitle" class="text-danger small italic" />
                                    </div>
                                    <div class="col-md-6">
                                        <label class="font-serif fw-bold text-brown-dark">Author</label>
                                        <InputText @bind-Value="NewBook.BookAuthor" class="form-control ink-input" />
                                        <ValidationMessage For="() => NewBook.BookAuthor" class="text-danger small italic" />
                                    </div>
                                </div>
                            </div>

                            @* II. Detalii Tehnice *@
                            <div class="col-md-4">
                                <label class="font-serif fw-bold text-brown-dark"><i class="bi bi-house me-2"></i>II. Publishing House</label>
                                <InputText @bind-Value="NewBook.BookPublishingHouse" class="form-control ink-input" />
                            </div>
                            <div class="col-md-2">
                                <label class="font-serif fw-bold text-brown-dark">Year</label>
                                <InputNumber @bind-Value="NewBook.BookYear" class="form-control ink-input" />
                            </div>
                            <div class="col-md-3">
                                <label class="font-serif fw-bold text-brown-dark">Language</label>
                                <InputText @bind-Value="NewBook.BookLanguage" class="form-control ink-input" />
                            </div>
                            <div class="col-md-3">
                                <label class="font-serif fw-bold text-brown-dark">Pages</label>
                                <InputNumber @bind-Value="NewBook.BookNumberPag" class="form-control ink-input" />
                                <ValidationMessage For="() => NewBook.BookNumberPag" class="text-danger small" />
                            </div>

                            @* III. Stare și Preț *@
                            <div class="col-md-6">
                                <label class="font-serif fw-bold text-brown-dark"><i class="bi bi-shield-check me-2"></i>III. Preservation State</label>
                                <InputSelect @bind-Value="NewBook.Condition" class="form-select ink-input">
                                    <option value="">-- Select Condition --</option>
                                    <option value="Mint">Mint (Pristine)</option>
                                    <option value="Fine">Fine (Light wear)</option>
                                    <option value="Good">Good (Aged but intact)</option>
                                    <option value="Fair">Fair (Significant wear)</option>
                                    <option value="Poor">Poor (Fragile/Damaged)</option>
                                </InputSelect>
                            </div>
                            <div class="col-md-6">
                                <label class="font-serif fw-bold text-brown-dark"><i class="bi bi-currency-exchange me-2"></i>IV. Valuation (RON)</label>
                                <div class="input-group">
                                    <InputNumber @bind-Value="NewBook.BookPrice" class="form-control ink-input" />
                                    <span class="input-group-text ink-input-addon">RON</span>
                                </div>
                            </div>

                            @* IV. Vizual și Categorie *@
                            <div class="col-md-6">
                                <label class="font-serif fw-bold text-brown-dark"><i class="bi bi-camera me-2"></i>V. Visual Capture (URL)</label>
                                <InputText @bind-Value="NewBook.BookCoverPath" class="form-control ink-input" placeholder="http://..." />
                            </div>
                            <div class="col-md-6">
                                <label class="font-serif fw-bold text-brown-dark">Category</label>
                                <InputSelect @bind-Value="NewBook.BookCategory" class="form-select ink-input">
                                    <option value="">-- Choose Category --</option>
                                    <option value="Fiction">Fiction</option>
                                    <option value="Philosophy">Philosophy</option>
                                    <option value="History">History</option>
                                    <option value="Rare">Rare Artifact</option>
                                </InputSelect>
                            </div>

                            @* V. Descriere *@
                            <div class="col-12">
                                <label class="font-serif fw-bold text-brown-dark"><i class="bi bi-feather me-2"></i>VI. Curator's Observations</label>
                                <InputTextArea @bind-Value="NewBook.BookDescription" class="form-control ink-input" rows="4" />
                            </div>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mt-5 pt-4 border-top border-dark">
                            <span class="small italic text-muted">Curator ID: @NewBook.SellerId</span>
                            <div class="d-flex gap-3">
                                <button type="submit" class="btn-wax-seal px-5 py-2">
                                    Seal & Register
                                </button>
                            </div>
                        </div>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    @@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=IM+Fell+English:ital@0;1&family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap');

    body {
        background-image: url('https://www.transparenttextures.com/patterns/dark-wood.png'), linear-gradient(to bottom, #2c1e14, #3d2b1f);
        background-attachment: fixed;
    }

    .inventory-scroll {
        background-color: #f2e8d5;
        background-image: radial-gradient(circle, rgba(255,255,255,0.2) 0%, rgba(0,0,0,0.05) 100%), url('https://www.transparenttextures.com/patterns/old-map.png');
        border: 1px solid #d4c5a9;
        box-shadow: 0 30px 60px rgba(0,0,0,0.8);
        color: #1a120b;
        border-radius: 4px;
        position: relative;
    }

    .archive-label {
        font-family: 'Cinzel', serif;
        color: #8b6b3e;
        letter-spacing: 3px;
        font-weight: 700;
        font-size: 0.9rem;
    }

    .archive-title {
        font-family: 'IM Fell English', serif;
        font-size: 2.5rem;
        font-weight: bold;
        color: #1a120b;
    }

    .archive-author {
        font-family: 'Playfair Display', serif;
        font-style: italic;
        color: #5a3e2b;
    }

    .registry-section-title {
        font-family: 'Cinzel', serif;
        color: #1a120b;
        border-bottom: 1px solid #8b6b3e;
        padding-bottom: 5px;
        margin-top: 15px;
        margin-bottom: 20px;
    }

    .text-brown-dark {
        color: #3d2b1f;
    }

    .ink-input {
        background: rgba(255, 255, 255, 0.4) !important;
        border: none !important;
        border-bottom: 1px solid #8b6b3e !important;
        border-radius: 0 !important;
        color: #1a120b !important;
        padding-left: 0 !important;
        font-family: 'Playfair Display', serif;
    }

        .ink-input:focus {
            background: rgba(255, 255, 255, 0.6) !important;
            box-shadow: none !important;
            border-bottom: 2px solid #1a120b !important;
        }

    .ink-input-addon {
        background: transparent !important;
        border: none !important;
        border-bottom: 1px solid #8b6b3e !important;
        border-radius: 0 !important;
        color: #8b6b3e;
        font-family: 'Cinzel', serif;
    }

    .border-dashed {
        border: 1px dashed #8b6b3e !important;
    }

    .alert-sepia {
        background-color: rgba(139, 107, 62, 0.1);
        border: 1px solid #8b6b3e;
        color: #5a3e2b;
        font-family: 'Playfair Display', serif;
    }

    .btn-ink-sm {
        background: transparent;
        border: 1px solid #8b6b3e;
        color: #8b6b3e;
        font-family: 'Cinzel', serif;
        font-size: 0.7rem;
        padding: 2px 10px;
        transition: 0.3s;
    }

        .btn-ink-sm:hover {
            background: #8b6b3e;
            color: #f2e8d5;
        }

    .btn-wax-seal {
        background: #7a0000;
        color: #fff;
        border: none;
        font-family: 'Cinzel', serif;
        box-shadow: 0 4px 10px rgba(0,0,0,0.3);
        transition: 0.3s;
    }

        .btn-wax-seal:hover {
            background: #900000;
            transform: translateY(-2px);
        }

    .btn-scroll-edit {
        border: 1px solid #2c1e14;
        color: #2c1e14;
        font-family: 'Cinzel', serif;
    }

    .btn-quill {
        color: #d4c5a9;
        font-family: 'Cinzel', serif;
    }

    .wax-seal-switch .form-check-input:checked {
        background-color: #7a0000;
        border-color: #500000;
    }
</style>

@code {
    [SupplyParameterFromForm]
    private Book NewBook { get; set; } = new() { BookStock = 1, BookRating = 0 };

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;

        var userIdClaim = user.FindFirst(ClaimTypes.NameIdentifier)?.Value
                          ?? user.FindFirst("sub")?.Value
                          ?? user.FindFirst("UserId")?.Value;

        if (int.TryParse(userIdClaim, out var userId))
        {
            NewBook.SellerId = userId;
        }
        else if (!user.Identity?.IsAuthenticated ?? true)
        {
            NavigationManager.NavigateTo("/login");
        }
    }

    private async Task HandleSubmit()
    {
        using var context = DbFactory.CreateDbContext();

        try
        {
            if (NewBook.SellerId != 0)
            {
                context.Book.Add(NewBook);
                await context.SaveChangesAsync();
                NavigationManager.NavigateTo("/antique");
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error: {ex.Message}");
        }
    }
}
